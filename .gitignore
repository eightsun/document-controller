# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js build output — never commit these
.next/
out/
build/

# Environment variables — contains secrets, never commit
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# TypeScript
next-env.d.ts
*.tsbuildinfo

# Package lock (optional — remove this line if you want to commit it)
# package-lock.json

# Claude Code local IDE settings (machine-specific, not for repo)
.claude/settings.local.json

# Misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
